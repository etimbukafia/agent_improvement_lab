"""Optional integrations for external agent runtimes.

The package intentionally does not import an integration. Import an adapter
from its concrete subpackage when the external runtime is installed.
"""
